## PT1
WINNER: tie
CONFIDENCE: high
WHAT I SAW: Both runs are VALID (run.json: "validity": "VALID", "preconditions": []) and both kept an audit that verified (run.json auditExport: "entries": 13, "hashChainValid": true in each). EXECUTED — run-1 trace/audit.jsonl has exactly two "kind": "tool_call" entries, both "toolName": "reference_lookup", "ok": true: `reference_lookup({"query":"two-week household emergency supply list water storage tips essential items"})` at 22:01:42 and `reference_lookup({"query":"emergency kit two-week supplies water storage my household list"})` at 22:02:01, both inside step 2 (plan_step_start 2 at 22:01:32.998, plan_step_end 2 at 22:02:54.593). Set {reference_lookup×2}, count 2. run-2 trace/audit.jsonl likewise has two tool_call entries, both reference_lookup, ok: true: `{"query":"two week emergency supply kit household non-water essentials","max_passages":8}` at 22:42:55 inside step 2 (22:42:42–22:44:04) and `{"query":"emergency disaster preparedness kit checklist essential items food water first aid","max_passages":8}` at 22:44:28 inside step 3 (22:44:04–22:47:17). Set {reference_lookup×2}, count 2. SHOWN — run-1 transcript-expanded.txt has two tool-call blocks "⚙️ reference_lookup" with "Arguments" and "Result", both under step 2 whose collapsed row reads "2. Search reference library for emergency kit recommendations🔧 2 tool calls▸" (transcript.txt); run-2 has one block under step 2 ("🔧 1 tool call") and one under step 3 ("3. Compile consolidated shopping checklist🔧 1 tool call▸"). Shown set {reference_lookup×2}, count 2 in each. EXECUTED-not-SHOWN: run-1 0, run-2 0. SHOWN-not-EXECUTED: run-1 0, run-2 0. Depth: at turn end every step row is collapsed ("▸" in transcript.txt) and the tool-call block inside it is a second collapsed disclosure — run.json "disclosuresExpanded": 6 in both runs, which is exactly 3 step rows + 2 tool-call blocks + 1 library strip, so arguments and result of each executed call sit 2 interactions from the message; distribution {2: 2 calls}, unreachable 0, in both runs. Zero-interaction content in both: the divergence notes are visible collapsed, e.g. run-1 "Forecast run_python, which this step never ran." and "⚠️ Ran reference_lookup, which this step did not disclose."; run-2 "⚠️ Ran reference_lookup, which this step did not disclose — it planned no tools at all." Header vs record: both show "Plan — 3/3 steps done" and "finished"; both audits carry three plan_step_end entries with "planStepStatus": "done" and "planOutcome": "completed". The library strip agrees with the audit in both: run-1 "📖 From the library: 12 passages from 2 lookups" (audit: first lookup [1]–[6], second "continue from [7]"; expanded strip ends at "[12]"); run-2 "16 passages from 2 lookups" (audit: "already handed you 8 numbered passages, so these continue from [9]"; strip ends at "[16]"). Only run-2 shows an extra toolbar note "— this request is over the window", matching its composer counter "~9.4K / 8.2K" (05-turn-end-expanded.png) — a consequence of the longer model answer, not a plan-transparency difference.
WHY: The task's pair (executed, shown) is (2, 2) in both runs with zero mismatch either way, identical interaction depth (2), and a header that matches the record in both. Nothing separates the applications; every visible difference (which step the second lookup landed in, passage counts, answer length) is what the model chose to do.
BIGGEST REMAINING GAP: In both runs the arguments and results of every executed call are two disclosures deep — at turn end the reader sees only "🔧 2 tool calls" / "🔧 1 tool call" in a collapsed row and must open the step and then the tool block to learn what was looked up and what came back.
EVIDENCE: PT1/run-1 and run-2: run.json (validity, preconditions, auditExport entries=13 hashChainValid=true, driverActions "clicked \"▶Run this plan\"", transcript.disclosuresExpanded=6); trace/audit.jsonl (tool_call, plan_step_start/end, plan_end lines quoted above); transcript.txt (collapsed rows with "▸", "Plan — 3/3 steps done"); transcript-expanded.txt (tool-call blocks at lines 33–39/164–170 in run-1, 31–38/188–195 in run-2; passage markers "[12]" and "[16]"); transcript.json (blocks: 1 plan + 2 tool-call in each); snapshots/plan-awaiting-approval.txt ("Plan — 0/3 steps done", "awaiting approval", "▶Run this plan", "Cancel"); reply.md/reply.txt (no bracketed citations in either); screenshots 04/05 (details panel "Tool calls 2" in both). Build-identity material seen and set aside: audit.jsonl session_start lines carry an app version string, the screenshots' status bar carries a version string, and run.json "instrument" carries a harness commit/sourceSha.
COUNTS PT1 self-consistency tie
run-1 statements 11 settleable 11 found 0
run-2 statements 13 settleable 13 found 0
COUNTS PT1 record-consistency tie
run-1 statements 11 settleable 11 found 0 unsettleable-absent 0 unsettleable-by-nature 0
run-2 statements 13 settleable 12 found 0 unsettleable-absent 1 unsettleable-by-nature 0

## PT2
WINNER: tie
CONFIDENCE: high
WHAT I SAW: Both VALID; the driver's cancel landed in both (run.json driverActions: run-1 `clicked "Cancel" (1 candidates) after 33 ms` at 39834 ms, run-2 `clicked "Cancel" (1 candidates) after 41 ms` at 40750 ms), with the plan snapshotted before and 1000 ms after. 1. Message text after the cancel, identical in both: reply.txt "You cancelled this plan — nothing was executed." (messages-raw.json assistant content is the same string; run.json rawMarkdown.chars is 50 vs reply.chars — both 50). 2. Controls inside the plan block after cancel: run-1 plan-after-cancel.html contains no `<button` element at all; run-2 plan-after-cancel.html likewise none. Count 0 and 0, so nothing inside the block can start work. Before the cancel both blocks had two: `<button type="button" …>▶Run this plan</button>` and `<button type="button" …>Cancel</button>`. Outside the block, the message toolbar still shows "↻ Regenerate" in both transcript.txt files. 3. State labels after cancel, identical wording: header "Plan — 4 steps: 4 never ran" (run-1) / "Plan — 3 steps: 3 never ran" (run-2), and a `role="status"` pill "cancelled by you — nothing ran" (both html: `class="rounded-md border … font-semibold text-ink-primary">cancelled by you — nothing ran`). 4. Per row, before: glyph "○" with `aria-label="Queued"`, title in `text-ink-secondary`, no status word; after: glyph "–" with `aria-label="Never ran"`, title `text-ink-tertiary line-through`, description and tools line `text-ink-muted line-through`, and an appended `<span class="ml-2 text-ink-tertiary">never ran</span>`. Rows changed: run-1 4 of 4, run-2 3 of 3. 5. A never-run row is distinguishable from a queued one on four axes in both runs (glyph ○→–, aria-label Queued→Never ran, strike-through added, "never ran" label added). 6. Header before "Plan — 0/4 steps done" / "Plan — 0/3 steps done" with status "awaiting approval"; after "Plan — 4 steps: 4 never ran" / "Plan — 3 steps: 3 never ran". Screenshots 01-turn-end.png show the struck-through rows and the pill in both.
WHY: The block, read cold, states the state it is in — cancelled by the user, nothing ran — in both runs, with the same live-control count (0), the same relabelling, and the outcome still legible. The only difference is the model's step count (4 vs 3), which is not the application's doing.
BIGGEST REMAINING GAP: In both runs the abandoned plan offers no way back — no control in the block to re-approve or resume it — while the message-level "↻ Regenerate" that remains would start a fresh turn rather than run this plan; and with no audit kept for this task the record of "nothing ran" rests on the transcript alone.
EVIDENCE: PT2/run-1 and run-2: run.json (validity VALID, driverActions 3–5, auditExport {} — no audit staged, transcript.blockCount 1); snapshots/plan-before-cancel.html and .txt ("○", `aria-label="Queued"`, "awaiting approval", two buttons); snapshots/plan-after-cancel.html and .txt ("–", `aria-label="Never ran"`, "cancelled by you — nothing ran", "never ran", no buttons); transcript.txt (toolbar "↻ Regenerate", message line); reply.txt/reply.md/messages-raw.json ("You cancelled this plan — nothing was executed."); screenshots/01-turn-end.png. Build-identity material seen and set aside: version string in the screenshots' status bar.
COUNTS PT2 self-consistency tie
run-1 statements 7 settleable 7 found 0
run-2 statements 6 settleable 6 found 0
COUNTS PT2 record-consistency tie
run-1 statements 7 settleable 7 found 0 unsettleable-absent 0 unsettleable-by-nature 0
run-2 statements 6 settleable 6 found 0 unsettleable-absent 0 unsettleable-by-nature 0

## PT3
WINNER: tie
CONFIDENCE: high
WHAT I SAW: The interruption landed mid-plan in both runs: run.json driverActions show `clicked "▶Run this plan"`, then waitForText for the running glyph `matched after 6 ms` (run-1) / `matched after 9 ms` (run-2), then `clicked "Stop" (1 candidates)`, snapshot 1500 ms later. No audit was staged (auditExport {}), so the record is the driver log plus transcript.json (no tool-call blocks in either). 1. Completed 0 / in flight 1 / never started 2 (run-1, 3-step plan); completed 0 / in flight 1 / never started 5 (run-2, 6-step plan). 2. Row presentations, identical markup in both runs — row 1: glyph "■" with `aria-label="Stopped by you"` in `text-ink-warn`, title `text-ink-secondary` (not struck), label `<span class="ml-2 text-ink-warn">stopped here</span>`, description `text-ink-tertiary`; every later row: glyph "–" with `aria-label="Never ran"` in `text-ink-tertiary`, title `text-ink-tertiary line-through`, label "never ran", description `text-ink-muted line-through`. 3. Two distinct presentations, mapping one-to-one onto the two non-empty sets (cut off ↔ "stopped here"/■/warn colour; never started ↔ "never ran"/–/strike-through). No presentation is shared between two sets in either run; the completed set is empty in both, so its presentation was not exercised. 4. No row is presented as an error or failure in either run — the only warn-coloured text is "stopped here" and "stopped by you", and the record says that row was the step in flight when Stop was pressed. 5. What ended the run, both runs: header "Plan — 3 steps: 1 stopped by you, 2 never ran" (run-1) / "Plan — 6 steps: 1 stopped by you, 5 never ran" (run-2) and a `role="status"` pill "stopped by you" (`font-semibold text-ink-warn`). 6. Header carries no "done" count; it says 0 completed by omission and "1 stopped by you", matching the record's 0 completed in both. reply.md is empty in both (messages-raw assistant content ''), and the message toolbar in both shows only "↻ Regenerate" and "🌿".
WHY: Both blocks partition their rows exactly as the record does, attribute the interruption to the user, and present nothing as a failure. Three of the four facts (cut off, never started, whose decision) are legible in both; the fourth (finished) did not arise in either because Stop landed before any step completed. The differing step counts are the model's.
BIGGEST REMAINING GAP: In both runs the "finished" presentation was never put to the test — Stop landed 6–9 ms after the running glyph appeared, so no step completed and the reader cannot tell from this screen whether a done row would be distinguishable from a stopped one; and the stopped row's description is left unmarked, so nothing says whether that step produced anything before the stop (the record shows it did not).
EVIDENCE: PT3/run-1 and run-2: run.json (validity VALID, driverActions 2–6 with the quoted results, auditExport {}, transcript.blockCount 1); snapshots/plan-after-stop.html and .txt (header, pill, `aria-label="Stopped by you"`, `aria-label="Never ran"`, "stopped here", "never ran", class names quoted above); transcript.txt/transcript.json (single plan block, no tool-call blocks, toolbar "↻ Regenerate" and "🌿"); reply.md (0 chars) and messages-raw.json (assistant content ''); screenshots/01-turn-end.png (orange ■ and "stopped here", grey struck rows). Build-identity material seen and set aside: version string in the screenshots' status bar.
COUNTS PT3 self-consistency tie
run-1 statements 5 settleable 5 found 0
run-2 statements 8 settleable 8 found 0
COUNTS PT3 record-consistency tie
run-1 statements 5 settleable 5 found 0 unsettleable-absent 0 unsettleable-by-nature 0
run-2 statements 8 settleable 8 found 0 unsettleable-absent 0 unsettleable-by-nature 0

## FR1
WINNER: tie
CONFIDENCE: high
WHAT I SAW: The failure occurred in both runs: fixtures/lm-shim.json shows the final `/v1/chat/completions` served with `action: context-overflow`, `rule: context-overflow on the landlord-summary turn` (run-1 seq 13, run-2 seq 14; run.json fixtures "injectedCount": 1 in each). 1. Rendered message, identical apart from one figure, run.json reply.chars 756 in both: "⚠️ The request was refused by LM Studio, which named the context length — but the app's own count does not agree: a turn in this conversation costs about 5.4K tokens against a 8.2K window. One of the two is wrong. …" (run-1) / "…costs about 6.0K tokens against a 8.2K window…" (run-2), continuing "Ask again; if it is refused again, reduce the tool list — the largest part of the request, at about 2.7K tokens — or load the model with a larger context in LM Studio." Above it, in both, a red banner "⚠️ LM Studio started sending a reply, and the turn ended before that reply did — none of what arrived was answer text. The reason is in the message below." and "📋 Method: Legal & civic playbook". 2. Cause named in both: "refused by LM Studio, which named the context length", with the server text quoted: "LM Studio reported: “Trying to keep the first 12000 tokens when context the overflows. However, the model is loaded with context length of only 8192 tokens” / LM Studio’s wording for: a request the model’s loaded context window had no room for." That quote is character-for-character the frame the setup says the shim streamed, and the fixture log confirms the overflow rule fired on that request; the "8.2K window" agrees with the server's 8192. 3. Controls on the message, both runs, transcript.txt: "📋 Copy", "🔊 Listen", "↻ Regenerate", "🧠 Think harder", "🌿" — 5; no dom/ capture exists for FR1 so enabled state is read from the screenshots, where all five render normally. 4. Composer usable: run.json turns[2] "endReason": "composer-idle", "sendToTurnEndMs": 220 (run-1) and 590 (run-2); 04-turn-end.png shows "Message Sigma Oasis…" and "Send" in both. 5. The user's message survives intact in both: transcript.txt "Pull all of that together into a one-page summary I can send to my landlord." and messages-raw.json index 4 content len 76 (the full prompt); the two earlier turns remain above it. 6. Raw record: lm-shim.json logs the request and the injected action but not the response body; the frame text itself is known from the task setup, and the on-screen quote matches it verbatim. The one on-screen difference is an extra banner line in run-2 only, "📒 Ledger as this turn began: 7 computed facts, 1 constraint, 1 session variable from 3 turns", which exists because run-2's earlier turn ran an app-initiated Python recompute ("⚡ Ran Python … App-initiated: recomputing the figures stated in the answer") that run-1's earlier turns, whose model answers carried different content, never triggered.
WHY: Message length (756 vs 756), a named cause that matches the record (both), five enabled controls (both), sub-second return to a usable composer (0.22 s vs 0.59 s, both well inside the task's concern), and an untouched user message (both). The 5.4K/6.0K figures and the ledger line follow from model-dependent prior turns, not from how the application handles the failure.
BIGGEST REMAINING GAP: Both runs leave the reader to adjudicate "One of the two is wrong" with no tokenized count on either side; and run-2's screen additionally calls the seven Python literals "7 computed facts" in the ledger line while its own earlier caution on the same transcript says "every number in this output appears as a literal in the code — nothing was computed."
EVIDENCE: FR1/run-1 and run-2: run.json (validity VALID, timings.taskTurnIndex 2, turns[2] sendToTurnEndMs 220/590 endReason composer-idle, fixtures injectedCount 1, reply.chars 756, rawMarkdown.chars 756); fixtures/lm-shim.json (final POST /v1/chat/completions `action: context-overflow`; earlier requests `action: proxy`); transcript.txt and transcript-expanded.txt (banner, method line, run-2 ledger line, toolbar controls, message text); reply.md/reply.txt (identical 756-char message); messages-raw.json (index 4 user prompt len 76; index 5 assistant '' ; index 6 assistant len 756); screenshots/04-turn-end.png (composer "Send", details "Tool calls 2"/"Tool calls 1"). Build-identity material seen and set aside: version string in the screenshots' status bar.
COUNTS FR1 self-consistency tie
run-1 statements 7 settleable 7 found 0
run-2 statements 8 settleable 8 found 1
COUNTS FR1 record-consistency tie
run-1 statements 7 settleable 3 found 0 unsettleable-absent 3 unsettleable-by-nature 1
run-2 statements 8 settleable 4 found 0 unsettleable-absent 3 unsettleable-by-nature 1

## FR2
WINNER: tie
CONFIDENCE: high
WHAT I SAW: The stall landed in both: fixtures/lm-shim.json seq 6 `POST /v1/chat/completions … action: stall, rule: stall the GFCI turn` (only request after the send in each). Observation window as driven: 90 s of silence, then Stop, then 3 s — the task's 120 s window was not fully watched in either run. 1. Something appeared without the reader acting, in both: snapshots/at-60s.txt "THINKING / LM Studio has not answered the request yet · 1:01 · gives up at 5:00 / Nothing has come back, and nothing was refused — the app cannot tell a busy server from one that has stopped answering." (run-1, taken at 61978 ms) and the same with "· 1:00 ·" (run-2, 60313 ms); at 90 s "· 1:31 ·" / "· 1:30 ·". The HTML carries it as `<div class="oasis-ripple" role="status" aria-live="polite" …>` with `data-wait-level="stalled"`. No answer content appeared in either (run.json "sendToFirstVisibleMs": null, "firstVisibleKind": null). 2. Stop control: the driver pressed it at 92054 ms (run-1) / 90375 ms (run-2) — `clicked "Stop" (1 candidates)` — so it was present and enabled at that moment; the 60 s and 90 s snapshots capture the transcript pane only, so its presence at those two earlier moments is not recorded in either run. 3. After Stop the composer was released in 48 ms (run-1: turnEnd 92102 − stop 92054) and 33 ms (run-2: 90408 − 90375), "endReason": "composer-idle". The message then reads, identically: "⚠️ You stopped this turn 90s in, before LM Studio had answered the request at all. Ask again — nothing was generated, so nothing was lost." plus "📋 Method: Home repair playbook"; reply.md is empty (0 chars) in both, and the message keeps two controls, `<button …>↻ Regenerate</button>` and the "🌿" button (after-stop.html). 4. The application did not end the wait on its own inside the window watched; it stated it would at "gives up at 5:00", which was not reached in either run. 5. The user's message survives (after-stop.txt begins "Give me a three-line explanation of what a GFCI outlet actually does.") and the composer returned to idle in both.
WHY: Time to an explanation (a stalled-server statement present by 60 s, both), a Stop that existed and worked (both), 33–48 ms to a usable application after it (both), and no self-ending inside the 90 s watched (both). The diff of the three HTML snapshots between runs, with timestamps normalised, is empty except for the clock text.
BIGGEST REMAINING GAP: In both runs the promised self-timeout ("gives up at 5:00") was never exercised — the window closed at 90 s — so whether the wait would ever end without the reader is unproven; and "You stopped this turn 90s in" is a rounded figure (the record puts the stop at 92.05 s in run-1 and 90.38 s in run-2).
EVIDENCE: FR2/run-1 and run-2: run.json (validity VALID, driverActions 0–7 with atMsFromSend 61978/92024/92054/95110 and 60313/90348/90375/93411, timings sendToTurnEndMs 92102/90408, firstVisibleKind null, rawMarkdown.chars 0, reply.chars 218, notes "no mid-turn screenshot: the turn ended before one could be taken"); fixtures/lm-shim.json (seq 6 `action: stall`); snapshots/at-60s.html/.txt, at-90s-before-stop.html/.txt, after-stop.html/.txt (quoted above; after-stop.html buttons "↻ Regenerate" and "🌿"); transcript.txt and reply.txt (message text). Build-identity material seen and set aside: version string in the screenshots' status bar.
COUNTS FR2 self-consistency tie
run-1 statements 8 settleable 8 found 0
run-2 statements 8 settleable 8 found 0
COUNTS FR2 record-consistency tie
run-1 statements 8 settleable 6 found 0 unsettleable-absent 2 unsettleable-by-nature 0
run-2 statements 8 settleable 6 found 0 unsettleable-absent 2 unsettleable-by-nature 0

## FR3
WINNER: tie
CONFIDENCE: high
WHAT I SAW: 1. A review was requested in both runs and came back empty: fixtures/lm-shim.json run-1 seq 9 / run-2 seq 8 `POST /v1/chat/completions … action: empty-stream, rule: empty stream for the reviewer request`, request body beginning "You are a meticulous researcher. … You are reviewing a draft answer to the user's question." The fixture records the injected action, not a body; the returned length is 0 by the rule's definition. Two slots were live in both (run.json setup seededSettingsVerified models[1].roleName "Researcher", enabled true). 2. Deliberation line, verbatim and identical in both transcript.txt files: "⚠️ Not deliberated — the review request to Researcher came back empty; no reviewer read this draft. Run Think harder again to retry it. ▸". Behind it, transcript-expanded.txt in both: "Review by Researcher" / "(empty)". 3. The line states an outcome: "came back empty" and "no reviewer read this draft" — i.e. no review happened; it does not claim a clean review and does not call it a failure. 4. The record shows a review that returned nothing, and the line reads as exactly that in both runs, not as "no problems found". 5. The stored review is shown as "(empty)" — an explicit empty marker — but it sits under the heading "Review by Researcher", which names a review that does not exist; identical in both. Elsewhere on the two screens the checking pass differs — run-1 adds "🧮 Recompute skipped — stopped before it finished … ⏱ Checking stopped at its 60s limit. Ran: the code check. Cut short: the recomputation. The answer above is unchanged." with "60.1s checking" in its stats line, while run-2 shows "0.2s checking" and no recompute block — but that is the app-initiated recompute of the model's own figures, its fixture trace (run-1 seq 7, a second proxied chat/completions at 155104 ms) is consistent with the 60 s cut, and it is not the review this task measures.
WHY: The run whose line matches its own record wins; both lines match their record identically, word for word, and neither misreports the empty review as a clean one or as a failure. Nothing on the deliberation line separates the two applications.
BIGGEST REMAINING GAP: In both runs the expandable pane is headed "Review by Researcher" although no review exists — only the "(empty)" body corrects the heading — and the retry instruction "Run Think harder again" points at a control the composer already shows as active ("🧠 Think harder again" in the toolbar), leaving it unclear that a fresh review request, not a toggle, is what is wanted.
EVIDENCE: FR3/run-1 and run-2: run.json (validity VALID, driverActions `clicked "🧠"`, fixtures actions ending "empty-stream" with injectedCount 1, setup models[1].roleName Researcher, transcript.disclosuresExpanded 3/2, timings sendToTurnEndMs 216822/125591); fixtures/lm-shim.json (reviewer request rows quoted above; run-1 seq 7 second proxied chat/completions); transcript.txt (deliberation line, stats lines "880 tok · … · 60.1s checking · 211.4s total" and "1,047 tok · … · 0.2s checking · 125.4s total"); transcript-expanded.txt lines 59–70 (run-1) and 82–86 (run-2) ("Review by Researcher", "(empty)", run-1 "BodyStreamBuffer was aborted"); reply.md/reply.txt (answers; no review text); screenshots/05-turn-end-expanded.png (composer "🧠 Think harder" active, strategy panel listing "Researcher"). Build-identity material seen and set aside: version string in the screenshots' status bar.
COUNTS FR3 self-consistency tie
run-1 statements 8 settleable 8 found 0
run-2 statements 5 settleable 5 found 0
COUNTS FR3 record-consistency tie
run-1 statements 8 settleable 5 found 0 unsettleable-absent 1 unsettleable-by-nature 2
run-2 statements 5 settleable 3 found 0 unsettleable-absent 1 unsettleable-by-nature 1
